version: "2"
containers:
- name: shell
  image: gcr.io/instruqt-shadow/cloud-storage:2.0.0
  shell: /bin/bash
  memory: 512
gcp_projects:
- name: cloud-storage
  services:
  - storage-api.googleapis.com
  - storage-component.googleapis.com
